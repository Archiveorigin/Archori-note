# 题库使用说明

**说明**

```bash
本题库根据实验报告大部分内容出题，包含7个章节，包括变体题型。
本平台具备自动出题、环境预置和实时评测功能，帮助大家“在做中学”，即时检验学习成果。
```

**先决条件**

```markdown
在正式开启题库练习前，请保证你**正确进入了节点**，连接环境方法详见**《云环境使用方法》**
```



### 开始实验

每次开始一个新的实验前，**必须**先运行此命令

`start-env[编号]`

```bash
# 示例
[root@localhost ~] start-env1    # 开始实验一
[root@localhost ~] start-env5    # 开始实验五
```

### 提交评测

当你认为自己完成了当前章节所有题目要求后，运行此命令进行评分。

`test-env[编号]`

```bash
# 示例
[root@localhost ~] test-env1     # 评测实验一
```

### 全局重置

如果你把环境改乱了，或者想从头开始，或者题目全部做完评测完成了，可以使用此“后悔药”。

`clean-env-all`

```bash
# 示例
[root@localhost ~] clean-env-all # 重置全局练习环境
```

### 故障上报

如果你觉得你当前节点环境坏了，请执行

 `reboot`

重置环境，如果问题依旧，请记下你当前是哪个节点，上报给我处理。我将帮您重置环境。

### 敬告

本环境搭建较为仓促，目的为给大家提供方便的 Linux 环境与覆盖较为全面的期末考试题库练习，所有节点均给到大家最高管理权限（ root ），但请大家珍惜环境，不要执行破坏性命令，另外，所有节点均安装有行为审计，对于恶意破坏系统者，~~我直接给你枪毙~~。祝大家进步！



---- Powered By 自动化吃饭睡觉工程师

---- 技术支持 来自 王道涵老师