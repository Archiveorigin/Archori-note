# 云环境使用方法

**介绍**

```bash
# 本项目目的为用户提供纯净的，开箱即用的Linux实验环境。
```

**云环境信息**

```bash
# 完整的 RHEL9.2 发行版本 详情见 /etc/os-release
# 资源配额:
# vCPU Core  1
# Memory     2GiB
```

**题库信息**

```bash
# LabEnv-From-Archoi v1.0
根据实验报告大部分实验内容出题，包含自动出题，环境重置，实时评测功能，更多内容详见附件《题库使用说明》
```

**官方网站**

```bash
https://doc.citprobe.cn/ssh
```

**使用方法**

在 Windows / MacOS 中打开 CMD/Terminal 输入命令

```bash
ssh -p 22480 guest@mc.citprobe.cn
```

密码为 20240433 （学号前8位）进入跳板机器，见图

![截屏2025-12-17 20.04.39](/Users/arc/Library/Application Support/typora-user-images/截屏2025-12-17 20.04.39.png)

按照指示，此跳板机只允许执行三条命令：

```bash
ssh-check        # 查看可用环境状态
ssh              # 连接目标环境
exit             # 结束会话
```

**首先执行 ssh-check**，见图

![截屏2025-12-17 20.09.44](/Users/arc/Library/Application Support/typora-user-images/截屏2025-12-17 20.09.44.png)

```bash
# 状态解释
HOSTNAME   # 主机名
SESSIONS   # 连接数，表示这台机器正在被多少人用
STATUS     # 状态
```

建议：只连接绿色的机器

例如，想连接 node3 执行

```bash
ssh root@node3
```

然后看到终端界面，就可以开始执行任意linux命令了。



总结一句话就是：连接跳板 -> 执行 ssh-check 查看可用机器 -> 使用 ssh 连接。



### 故障

如遇环境故障，请执行 poweroff 再汇报给我重置环境。



### 另外

所有环境均安装有审计插件，请勿执行任何损害机器的命令，违者直接枪毙。