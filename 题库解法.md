# 题库解法



Annotation :

```bash
实现功能有效的方法有很多，此文档仅供参考。

                                    --- 自动化吃饭睡觉工程师
```



## 实验一

Task

```bash
========================================================
            实验一：系统安装与基础配置
========================================================
【题目要求】
1. 将主机名修改为: server-node
2. 设置系统时区为: Asia/Shanghai
3. 修改 /etc/motd 文件，添加一行欢迎语: 'Welcome to RHEL9 Lab'
4. 开启防火墙服务 (firewalld) 并设置为开机自启
--------------------------------------------------------
完成后，运行 test-env1 进行评测。
```

Resolve

```bash
# 修改主机名
[Arc@Env ~]: hostnamectl set-hostname server-node
# 修改时区
[Arc@Env ~]: timedatectl set-timezone Asia/Shanghai
# 添加欢迎语
[Arc@Env ~]: echo "Welcome to RHEL9 Lab" >> /etc/motd
# 开启防火墙
[Arc@Env ~]: systemctl enable firewalld --now
```



## 实验二

Task

```bash
========================================================
            实验二：常用命令操作 (综合加强版)
========================================================
【工作目录】: /opt/lab2_work
【题目要求】
1. 在工作目录下创建一个名为 'backup' 的目录。
2. 复制 data/server.log 到 backup 目录，并改名为 error_log.bak。
3. 【grep】筛选出 server.log 中包含 'ERROR' 的行，并重定向保存到 data/errors_only.txt。
4. 【wc】统计 server.log 文件一共有多少行，将数字写入 data/line_count.txt。
5. 【ln】为 data/server.log 创建一个软链接(符号链接)，放在工作目录根下，名为 link_log。
6. 【chmod】修改 data/server.log 的权限为：所有者读写(6)，组只读(4)，其他人无权限(0)。(即640)
7. 【find】在 /etc 目录下查找所有以 'host.conf' 结尾的文件，将路径保存到 find_res.txt。
8. 【tar】将目录下的 package.tar.gz 解压到 data/unzip_dir 目录中 (需自行创建该目录)。
--------------------------------------------------------
```

Resolve

```bash
[Arc@Env ~]: cd /opt/lab2_work
# 1
[Arc@Env lab2_work]: mkdir backup
# 2
[Arc@Env lab2_work]: cp data/server.log backup/error_log.bak
# 3
[Arc@Env lab2_work]: grep ERROR data/server.log >> data/errors_only.txt
# 4
[Arc@Env lab2_work]: wc -l data/server.log >> data/line_count.txt
# 5
[Arc@Env lab2_work]: ln -s data/server.log link_log
# 6
[Arc@Env lab2_work]: chmod 640 data/server.log 
# 7
[Arc@Env lab2_work]: find /etc -name host.conf >> find_res.txt
# 8
[Arc@Env lab2_work]: mkdir /data/unzip_dir
[Arc@Env lab2_work]: tar -xzvf package.tar.gz 
./
./config.xml
./readme.md
[Arc@Env lab2_work]: mv readme.md config.xml data/unzip_dir/
```



## 实验三

Task

```bash
========================================================
            实验三：Vim 编辑器实战
========================================================
【目标文件】: /root/vim_exam/practice.txt
【题目要求】
1. 将第 4 行的 'OLD_TEXT' 替换为 'NEW_TEXT'
2. 删除第 2 行 (包含 'delete this line' 的行)
3. 将第 5 行 (包含 'Copy me') 复制并在文件末尾粘贴
4. 将修改后的文件另存为 /root/vim_exam/final.txt
--------------------------------------------------------
```

Resolve

```bash
[Arc@Env lab2_work]: vim /root/vim_exam/practice.txt 
:4,4s/OLD_TEXT/NEW_TEXT  
# 光标移到第二行
dd
# 光标移到第五行
yy
# 光标移到末尾
p
:wq
[Arc@Env lab2_work]: mv /root/vim_exam/practice.txt /root/vim_exam/final.txt
```



## 实验四

Task

```bash
========================================================
            实验四：用户、组与权限管理
========================================================
【题目要求】
1. 创建一个名为 devgroup 的组
2. 创建用户 devuser，指定其主组为 devgroup
3. 为 devuser 设置密码 'redhat'
4. 锁定用户 devuser (使其无法登录)
5. 将目录 /opt/project_files 的属主更改为 devuser，属组更改为 devgroup
--------------------------------------------------------
```

Resolve

```bash
# 1
[Arc@Env lab2_work]: groupadd devgroup
# 2
[Arc@Env lab2_work]: useradd -g devgroup devuser
# 3
[Arc@Env lab2_work]: echo redhat | passwd --stdin devuser
更改用户 devuser 的密码 。
passwd：所有的身份验证令牌已经成功更新。
# 4
[Arc@Env lab2_work]: passwd -l devuser
锁定用户 devuser 的密码 。
passwd: 操作成功
# 5
[Arc@Env lab2_work]: chown devuser:devgroup /opt/project_files/
```



## 实验五

Task

```bash
========================================================
            实验五：磁盘分区与自动挂载
========================================================
【模拟磁盘设备】: /dev/loop1 (请操作此设备)
【题目要求】
1. 在该磁盘上创建第一个主分区(p1)，大小 500M，格式化为 ext4，挂载到 /mnt/data
2. 配置 /etc/fstab 实现开机自动挂载 /mnt/data
3. 在该磁盘上创建第二个分区(p2)，大小 256M，类型设置为 'Linux swap' (ID: 82)
4. 格式化 p2 为交换分区 (mkswap)，并启用它 (swapon)
--------------------------------------------------------
```

Resolve

```bash
# 1
[Arc@Env lab2_work]: fdisk /dev/loop1

欢迎使用 fdisk (util-linux 2.37.4)。
更改将停留在内存中，直到您决定将更改写入磁盘。
使用写入命令前请三思。

设备不包含可识别的分区表。
创建了一个磁盘标识符为 0x19ccbd07 的新 DOS 磁盘标签。

命令(输入 m 获取帮助)：n
分区类型
   p   主分区 (0 primary, 0 extended, 4 free)
   e   扩展分区 (逻辑分区容器)
选择 (默认 p)：p
分区号 (1-4, 默认  1): 
第一个扇区 (2048-4194303, 默认 2048): 
最后一个扇区，+/-sectors 或 +size{K,M,G,T,P} (2048-4194303, 默认 4194303): +500M

创建了一个新分区 1，类型为“Linux”，大小为 500 MiB。

命令(输入 m 获取帮助)：w
分区表已调整。
将调用 ioctl() 来重新读分区表。
正在同步磁盘。
# 重要的！！由于 /mnt 挂载了光盘文件，所以必须先卸载光盘，考试不会这样
[Arc@Env lab2_work]: umount /mnt     # 先卸载
[Arc@Env lab2_work]: mkdir /mnt/data # 再创建
[Arc@Env lab2_work]: mount /dev/loop1p1 /mnt/data
# 2
[Arc@Env lab2_work]: echo "/dev/loop1p1 /mnt/data ext4 defaults 0 0" >> /etc/fstab
# 3
[Arc@Env lab2_work]: fdisk /dev/loop1

欢迎使用 fdisk (util-linux 2.37.4)。
更改将停留在内存中，直到您决定将更改写入磁盘。
使用写入命令前请三思。

This disk is currently in use - repartitioning is probably a bad idea.
It's recommended to umount all file systems, and swapoff all swap
partitions on this disk.


命令(输入 m 获取帮助)：n
分区类型
   p   主分区 (1 primary, 0 extended, 3 free)
   e   扩展分区 (逻辑分区容器)
选择 (默认 p)：p
分区号 (2-4, 默认  2): 
第一个扇区 (1026048-4194303, 默认 1026048): 
最后一个扇区，+/-sectors 或 +size{K,M,G,T,P} (1026048-4194303, 默认 4194303): +256M

创建了一个新分区 2，类型为“Linux”，大小为 256 MiB。

命令(输入 m 获取帮助)：t
分区号 (1,2, 默认  2): 2
Hex 代码或别名（输入 L 列出所有代码）：82

已将分区“Linux”的类型更改为“Linux swap / Solaris”。

命令(输入 m 获取帮助)：w
分区表已调整。
正在同步磁盘。
# 4
[Arc@Env lab2_work]: mkswap /dev/loop1p2
正在设置交换空间版本 1，大小 = 256 MiB (268431360  个字节)
无标签，UUID=26954f15-213d-4541-9e28-1198bfcee23b
[Arc@Env lab2_work]: swapon /dev/loop1p2
```



## 实验六

Task

```bash
========================================================
            实验六：DHCP服务器配置 (文档定制版)
========================================================
【配置文件】: /etc/dhcp/dhcpd.conf
【场景】: 某公司内网 jnrplinux.com，DNS服务器为 192.168.1.2
【题目要求】
1. 设置 DHCP 作用域网段: 192.168.1.0，子网掩码: 255.255.255.0
2. 地址池范围 (range): 192.168.1.100 到 192.168.1.200
3. 设置域名 (option domain-name): "jnrplinux.com"
4. 设置DNS服务器 (option domain-name-servers): 192.168.1.2
5. 设置默认网关 (option routers): 192.168.1.1
6. 为特定主机配置固定IP (host boss):
   - MAC地址: 00:11:22:33:44:55
   - 固定IP: 192.168.1.88
--------------------------------------------------------
```

Resolve

```bash
# /etc/dhcp/dhcpd.conf

# 全局选项
option domain-name "jnrplinux.com";
option domain-name-servers 192.168.1.2;

default-lease-time 600;
max-lease-time 7200;

log-facility local7;

# 1. DHCP 作用域配置
subnet 192.168.1.0 netmask 255.255.255.0 {
  # 2. 地址池范围 (注意这里是空格，没逗号)
  range 192.168.1.100 192.168.1.200;
  
  # 5. 设置默认网关
  option routers 192.168.1.1;
}

# 6. 为特定主机配置固定IP (注意连字符 fixed-address)
host boss {
  hardware ethernet 00:11:22:33:44:55;
  fixed-address 192.168.1.88;
}
```



## 实验七

Task

```bash
========================================================
            实验七：DNS服务器配置 (文档定制版)
========================================================
【背景】: 企业域名 jnrplinux.com，DNS服务器 IP 为 192.168.1.2
【题目要求】
1. 修改 /etc/named.conf，监听所有端口，允许任何IP查询 (any)。
2. 配置正向区域: zone "jnrplinux.com"，文件名为 "jnrplinux.com.zone"
3. 配置反向区域: 针对 192.168.1.0/24 网段，文件名为 "192.168.1.zone"
4. 在正向文件 /var/named/jnrplinux.com.zone 中添加 A 记录：
   - dns.jnrplinux.com -> 192.168.1.2 (本机)
   - cw.jnrplinux.com  -> 192.168.1.11 (财务部)
   - xs.jnrplinux.com  -> 192.168.1.12 (销售部)
   - jl.jnrplinux.com  -> 192.168.1.13 (经理部)
   - oa.jnrplinux.com  -> 192.168.1.14 (OA系统)
5. 在反向文件 /var/named/192.168.1.zone 中添加对应的 PTR 记录。
--------------------------------------------------------
```

Resolve

```bash
# /etc/named.conf

//
// named.conf
//
// Provided by Red Hat bind package to configure the ISC BIND named(8) DNS
// server as a caching only nameserver (as a localhost DNS resolver only).
//
// See /usr/share/doc/bind*/sample/ for example named configuration files.
//

options {
	listen-on port 53 { any; };
	listen-on-v6 port 53 { any; };
	directory 	"/var/named";
	dump-file 	"/var/named/data/cache_dump.db";
	statistics-file "/var/named/data/named_stats.txt";
	memstatistics-file "/var/named/data/named_mem_stats.txt";
	secroots-file	"/var/named/data/named.secroots";
	recursing-file	"/var/named/data/named.recursing";
	allow-query     { any; };

	/* 
	 - If you are building an AUTHORITATIVE DNS server, do NOT enable recursion.
	 - If you are building a RECURSIVE (caching) DNS server, you need to enable 
	   recursion. 
	 - If your recursive DNS server has a public IP address, you MUST enable access 
	   control to limit queries to your legitimate users. Failing to do so will
	   cause your server to become part of large scale DNS amplification 
	   attacks. Implementing BCP38 within your network would greatly
	   reduce such attack surface 
	*/
	recursion yes;

	dnssec-validation yes;

	managed-keys-directory "/var/named/dynamic";
	geoip-directory "/usr/share/GeoIP";

	pid-file "/run/named/named.pid";
	session-keyfile "/run/named/session.key";

	/* https://fedoraproject.org/wiki/Changes/CryptoPolicy */
	include "/etc/crypto-policies/back-ends/bind.config";
};

logging {
        channel default_debug {
                file "data/named.run";
                severity dynamic;
        };
};

zone "jnrplinux.com" IN {
	type master;
	file "jnrplinux.com.zone";
};
zone "1.168.192.in-addr.arpa" IN {
	type master;
	file "192.168.1.zone";
};

include "/etc/named.rfc1912.zones";
include "/etc/named.root.key";
```

```bash
# /var/named/jnrplinux.com.zone

$TTL 1D
@       IN SOA  dns.jnrplinux.com. admin.jnrplinux.com. (
                                        0       ; serial
                                        1D      ; refresh
                                        1H      ; retry
                                        1W      ; expire
                                        3H )    ; minimum
        NS      dns.jnrplinux.com.
dns     A       192.168.1.2
cw      A       192.168.1.11
xs      A       192.168.1.12
jl      A       192.168.1.13
oa      A       192.168.1.14
```

```bash
# /var/named/192.168.1.zone

$TTL 1D
@       IN SOA  dns.jnrplinux.com. admin.jnrplinux.com. (
                                        0       ; serial
                                        1D      ; refresh
                                        1H      ; retry
                                        1W      ; expire
                                        3H )    ; minimum
        NS      dns.jnrplinux.com.
2       PTR     dns.jnrplinux.com.
11      PTR     cw.jnrplinux.com.
12      PTR     xs.jnrplinux.com.
13      PTR     jl.jnrplinux.com.
14      PTR     oa.jnrplinux.com.
```



​                                                                                   ---- Powered By 自动化吃饭睡觉工程师